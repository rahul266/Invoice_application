<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <h2><%= name %>'s invoices </h2>
    <form action="/update/<%= name %> " method="post">
        <% for( let i = 0; i < item.length; i++ ) { %>
            <table class="table">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">ITEM</th>
                    <th scope="col">ITEM COST</th>
                    <th scope="col">ITEM QUANTITY</th>
                    <th scope="col">AMOUNT</th>
                    <th scope="col">STATUS</th>
                  </tr>
                </thead>
                <tbody>
                <% var count=0 %> 
                <% var amount=0 %> 
                <% for( let j = 0; j < item[i].length; j++ ) { %>
                  <tr>
                    <th scope="row"><%= count+1 %> </th>
                    <td><%= item[i][j] %> </td>
                    <td><%= itemcost[i][j] %></td>
                    <td><%= itemquantity[i][j] %></td>
                    <td><%= itemcost[i][j]*itemquantity[i][j] %> </td>
                    <% amount+=itemcost[i][j]*itemquantity[i][j] %> 
                    <td> <%= status[i] %>  </td>
                    <% count+=1 %> 
                  </tr>
                   
                <% } %>
                </tbody>
              </table>
              <h4>Amount: <%= amount %> </h4>
              <label for=""><h4>status:</h4></label>
              <% if(status[i]==="due") {%>
                <button type="submit" name="number" value=<%= i %> >PAID</button>
              <% } %> 
              <% if(status[i]==="paid") { %>
                <button type="submit" name="number" value=<%= i %> >DUE</button>
              <% } %> 
        <% } %>
    </form>
</body>
</html>